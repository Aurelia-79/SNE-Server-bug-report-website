# 非礼勿视服务器管理系统 公网生产部署手册

## 1. 部署目标

将系统部署到公网域名下，供真实用户长期使用。

- 后端：FastAPI
- 前端：Vue 3 + Vite
- 数据库：MySQL
- 反向代理：Nginx
- 附件存储：本地磁盘持久化

## 2. 上线前准备

### 服务器要求

- Linux VPS 或云服务器
- Python 3.11+
- Node.js 20+
- MySQL 8+
- Nginx
- 已解析好的域名
- HTTPS 证书

### 推荐目录

```text
/opt/nls-admin/
  backend/
  frontend/
  data/
  logs/
```

## 3. 数据库准备

### 创建数据库

```sql
CREATE DATABASE server_admin_system DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'server_admin'@'%' IDENTIFIED BY '强密码';
GRANT ALL PRIVILEGES ON server_admin_system.* TO 'server_admin'@'%';
FLUSH PRIVILEGES;
```

### 初始化建议

- 第一次上线建议使用全新空库
- 如果是从本地 SQLite 迁移到生产 MySQL，先导出业务数据再导入
- 不要把开发库直接挂到公网

## 4. 后端部署

### 4.1 安装依赖

```bash
cd /opt/nls-admin/backend
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 4.2 配置环境变量

在 `backend/.env` 中填写：

```env
ENVIRONMENT=production
DATABASE_URL=mysql+pymysql://server_admin:强密码@127.0.0.1:3306/server_admin_system
SECRET_KEY=请换成随机长密钥
ACCESS_TOKEN_EXPIRE_MINUTES=720
CORS_ORIGINS=https://你的前端域名
TRUSTED_HOSTS=你的前端域名,你的后端域名
ENABLE_DOCS=0
SEED_DEMO_DATA=0
BOOTSTRAP_SUPER_ADMIN_USERNAME=superadmin
BOOTSTRAP_SUPER_ADMIN_PASSWORD=请换成强密码
BOOTSTRAP_SUPER_ADMIN_DISPLAY_NAME=系统超管
UPLOAD_DIR=/opt/nls-admin/data/uploads
DEFAULT_PASS_SCORE=60
MAX_UPLOAD_SIZE_MB=50
```

### 4.3 启动后端

```bash
cd /opt/nls-admin/backend
source .venv/bin/activate
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

### 4.4 systemd 服务

`/etc/systemd/system/nls-admin-backend.service`

```ini
[Unit]
Description=NLS Admin Backend
After=network.target mysql.service

[Service]
WorkingDirectory=/opt/nls-admin/backend
EnvironmentFile=/opt/nls-admin/backend/.env
ExecStart=/opt/nls-admin/backend/.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=3
User=www-data

[Install]
WantedBy=multi-user.target
```

启用：

```bash
sudo systemctl daemon-reload
sudo systemctl enable nls-admin-backend
sudo systemctl start nls-admin-backend
sudo systemctl status nls-admin-backend
```

## 5. 前端部署

### 5.1 安装依赖并构建

```bash
cd /opt/nls-admin/frontend
npm install
npm run build
```

### 5.2 前端环境变量

如果前后端同域名并由 Nginx 反代，前端可以不设置 `VITE_API_BASE_URL`。

如果前后端分域名，填写：

```env
VITE_API_BASE_URL=https://你的后端域名
```

### 5.3 Nginx 静态站点

```nginx
server {
    listen 80;
    server_name 你的前端域名;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name 你的前端域名;

    ssl_certificate     /etc/letsencrypt/live/你的域名/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/你的域名/privkey.pem;

    root /opt/nls-admin/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 6. HTTPS

建议使用 Let’s Encrypt：

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d 你的前端域名
```

## 7. 首次上线检查

按顺序检查：

1. 访问前端首页是否正常
2. 登录是否成功
3. `/health` 是否返回 `ok`
4. 注册是否能创建普通玩家
5. 系统超管是否显示为 `无部门`
6. 人事部是否能创建试卷
7. 选择题是否自动判分
8. 简答题是否能手动批改
9. Bug Report 附件是否可上传和下载

## 8. 备份策略

### 数据库备份

```bash
mysqldump -u server_admin -p server_admin_system > /opt/nls-admin/data/backup_$(date +%F).sql
```

### 附件备份

- 备份 `/opt/nls-admin/data/uploads`
- 与数据库备份保持同频

### 建议频率

- 数据库：每天
- 附件：每天
- 配置文件：每次改动后

## 9. 升级流程

1. 备份数据库和附件
2. 拉取新代码
3. 更新后端依赖
4. 执行前端重新构建
5. 重启后端服务
6. 检查健康接口和登录

## 10. 回滚流程

1. 停止后端服务
2. 恢复上一版代码
3. 恢复数据库备份
4. 恢复附件目录
5. 重启服务
6. 再次验证登录和核心功能

## 11. 安全建议

- `ENABLE_DOCS=0`
- `SEED_DEMO_DATA=0`
- `SECRET_KEY` 使用随机长密钥
- `TRUSTED_HOSTS` 只填真实域名
- 只开放 80/443 端口
- 定期更换初始超管密码
- 上传目录不要放在 Web 根目录下

## 12. 常见故障

### 502 Bad Gateway

- 检查后端服务是否启动
- 检查 `127.0.0.1:8000` 是否监听
- 查看 `systemd` 日志

### 403 / 登录失败

- 检查 `SECRET_KEY`
- 检查数据库中的用户密码是否正确
- 检查是否启用了 `TrustedHostMiddleware`

### 上传失败

- 检查 `UPLOAD_DIR` 权限
- 检查磁盘空间
- 检查 `MAX_UPLOAD_SIZE_MB`

### 注册后显示无部门

- 正常。普通玩家默认就是无部门。

### 系统超管无部门

- 正常。系统超管不属于任何部门。

