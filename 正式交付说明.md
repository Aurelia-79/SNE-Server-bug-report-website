# 非礼勿视管理系统正式版交付说明

## 目录结构

- `app.py`：正式启动入口。
- `server/`：FastAPI 后端代码。
- `templates/`：前端入口页面。
- `static/`：前端静态资源。
- `data/app.db`：默认 SQLite 数据库，首次启动自动创建。
- `data/uploads/`：上传附件目录。
- `.env`：正式环境配置，必须自行从 `.env.example` 复制生成。

## 交付清单

正式交付时至少保留这些文件和目录：

```text
app.py
server/
templates/
static/
requirements.txt
.env
.env.example
start_production.bat
run_background.bat
stop_server.bat
正式交付说明.md
```

`data/` 是运行数据目录。已有正式数据时必须一起备份和迁移；全新部署时可以不带，首次启动会自动创建。

## 首次部署

1. 复制配置：

```bat
copy .env.example .env
```

2. 编辑 `.env`，至少修改：

```env
SECRET_KEY=换成32位以上随机密钥
BOOTSTRAP_SUPER_ADMIN_PASSWORD=换成正式超管密码
```

3. 启动：

```bat
start_production.bat
```

4. 打开：

```text
本机访问：http://127.0.0.1:17250
局域网访问：http://服务器IP:17250
```

## 后台运行

```bat
run_background.bat
```

停止：

```bat
stop_server.bat
```

日志：

```text
logs/server.out.log
logs/server.err.log
```

## 默认超管

账号来自 `.env`：

```env
BOOTSTRAP_SUPER_ADMIN_USERNAME=superadmin
BOOTSTRAP_SUPER_ADMIN_PASSWORD=你设置的密码
```

超管只在数据库中不存在同名用户时创建。数据库已有 `superadmin` 时，改 `.env` 不会覆盖原密码。
